import React from 'react';

// Get the components associated with this page

// Media page
const Media = () => (
  <div>
    <div>
      <span>test</span>
    </div>
  </div>
);

export default Media;
